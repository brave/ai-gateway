# Rate Limiting

Rate limiting is permissive by default (no-op) and is intended to be backed by a private rate-limiting service in production deployments. `RATE_LIMITING_ENABLED` toggles whether the checks below run at all.
