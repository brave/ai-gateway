# .coveragerc to control coverage.py
[run]
source =
        aichat

[report]
# Only the aichat package counts toward the gate. (coverage resolves
# `source` differently across python versions; on some it also swallows
# test/aichat/*, which would inflate the numbers.)
include =
        aichat/*
precision = 1